import express from "express";
import path from "path";
import { createServer as createViteServer } from "vite";

async function startServer() {
  const app = express();
  const PORT = 3000;

  // Body parser
  app.use(express.json());

  // API Routes
  app.get("/api/health", (req, res) => {
    res.json({ status: "ok", timestamp: new Date().toISOString() });
  });

  // Example payment intent route (mocked for now)
  app.post("/api/payments/create-intent", (req, res) => {
    const { amount, listingId } = req.body;
    // In a real scenario, integrate with Stripe here
    res.json({ 
      clientSecret: "mock_secret_" + Math.random().toString(36).substring(7),
      amount,
      listingId
    });
  });

  // Vite middleware for development
  if (process.env.NODE_ENV !== "production") {
    const vite = await createViteServer({
      server: { middlewareMode: true },
      appType: "spa",
    });
    app.use(vite.middlewares);
  } else {
    const distPath = path.join(process.cwd(), "dist");
    app.use(express.static(distPath));
    app.get("*", (req, res) => {
      res.sendFile(path.join(distPath, "index.html"));
    });
  }

  app.listen(PORT, "0.0.0.0", () => {
    console.log(`Varoni Motos Server running on http://localhost:${PORT}`);
  });
}

startServer();
